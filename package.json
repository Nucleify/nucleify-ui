{
  "name": "nucleify-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "web-dev-server",
    "build": "tsc",
    "check": "biome check .",
    "write": "biome format --write .",
    "prepare": "husky"
  },
  "dependencies": {
    "lit": "^3.3.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0",
    "@web/dev-server": "^0.4.6",
    "@web/dev-server-esbuild": "^1.0.5",
    "husky": "^9.1.7",
    "typescript": "^5.8.3"
  }
}
